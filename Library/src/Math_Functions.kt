import  kotlin.math.*

fun main(args: Array<String>) {

    //math functions max,min,abs

    val maxno = max(1122, 23)
    println("Max number is $maxno")

    val minno = min(1122, 233)
    println("Min number is $minno")

    val absno = abs(-23)
    println("Abs number is $absno")
}